# Examples

Real saved sessions are written under each workspace's `.duet/` directory.

This directory includes a real seeded demo run:

- `seed-demo-session.json`
- `seed-demo-session.md`

To generate another one, run:

```bash
duet run --seed-demo --max-turns 6 --verify pytest
```
